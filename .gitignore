node_modules/
.next/
out/
.env*
# ...but the template carries no secrets and documents the variables the admin
# panel needs, so it has to be committed.
!.env.example
# The development store. Holds the contract address between `npm run dev`
# restarts; the deployed site uses Upstash instead.
.data/
.DS_Store
*.tsbuildinfo
next-env.d.ts
